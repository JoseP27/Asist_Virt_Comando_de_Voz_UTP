Dentro de las librerias utilziadas en este proyecto son:
pip install numpy
pip install tensorflow
pip install tflearn
pip install nltk
pip install flask
pip install jyserver

PD: el codigo mostrado aqui, son fragmentos del codigo funcional.
